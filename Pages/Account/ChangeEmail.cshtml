@page
@model BookStore.Pages.Account.ChangeEmailModel
@{
    ViewData["Title"] = "Thay đổi Email";
}

<div class="container py-4">
    <div class="row g-4">
        <!-- SIDEBAR -->
        <div class="col-lg-3">
            <partial name="_SidebarPartial" />
        </div>

        <!-- MAIN CONTENT -->
        <div class="col-lg-9">
            <div class="card border-0 shadow-sm rounded-4 p-4 bg-white">
                <h4 class="fw-bold mb-1 text-dark">Thay đổi địa chỉ Email</h4>
                <p class="text-muted small mb-4">Email hiện tại của bạn: <strong class="text-dark">@Model.CurrentEmail</strong></p>

                <form method="post" style="max-width: 500px;">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 mb-3" role="alert"></div>

                    <div class="mb-3">
                        <label asp-for="Input.NewEmail" class="form-label fw-semibold">Email mới</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fa-regular fa-envelope text-muted"></i></span>
                            <input asp-for="Input.NewEmail" type="email" class="form-control border-start-0 ps-0" placeholder="VD: newemail@domain.com" autofocus />
                        </div>
                        <span asp-validation-for="Input.NewEmail" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Input.Password" class="form-label fw-semibold">Xác nhận mật khẩu hiện tại</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-lock text-muted"></i></span>
                            <input asp-for="Input.Password" class="form-control border-start-0 ps-0" placeholder="Nhập mật khẩu của bạn" />
                        </div>
                        <span asp-validation-for="Input.Password" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-danger px-4 py-2 fw-bold rounded-3">
                            <i class="fa-solid fa-floppy-disk me-2"></i>Lưu Email mới
                        </button>
                        <a asp-page="/Account/Profile" class="btn btn-outline-secondary px-4 py-2 rounded-3">Hủy</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
